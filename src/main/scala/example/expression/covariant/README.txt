Go back and check to see if we have detected error.

interface CollectPrettyPSimplifyExpr extends Collect, PrettyP, SimplifyExpr { }

This must also extend CP, CS, and PS since they are proper subsets.